# Coding Standards Skill

Prefer small cohesive functions, descriptive names, type hints, explicit error handling, and minimal dependencies.
Follow the project's formatter/linter configuration.
Avoid duplication and hidden global state.
Do not commit secrets, generated junk, debug prints, or temporary test bypasses.
Treat tests, lint, types, and build as part of the definition of done.
